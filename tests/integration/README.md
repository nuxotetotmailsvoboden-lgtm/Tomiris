These tests are intentionally reserved for a real PostgreSQL instance. Run them with Docker Desktop or the CI PostgreSQL service; SQLite is prohibited for transaction, uniqueness, restart and concurrency coverage.

